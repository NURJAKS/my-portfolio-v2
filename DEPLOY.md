# Инструкция по деплою на Cloudflare Pages

## Быстрый старт

### Способ 1: Через GitHub + Cloudflare Pages (Рекомендуется) ⭐

1. **Подготовка к коммиту:**
   ```bash
   # Убедитесь, что вы в правильной директории
   cd vcard-personal-portfolio
   
   # Проверьте статус файлов
   git status
   
   # Добавьте все файлы (кроме игнорируемых)
   git add .
   
   # Создайте коммит
   git commit -m "Initial commit: Portfolio website ready for Cloudflare Pages"
   
   # Если репозиторий еще не создан на GitHub:
   # 1. Создайте новый репозиторий на GitHub
   # 2. Добавьте remote:
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   
   # Запушьте в GitHub
   git push -u origin main
   # или git push -u origin master (если используете master)
   ```

2. **Деплой на Cloudflare Pages:**
   - Зайдите на https://dash.cloudflare.com/
   - Перейдите в **Pages** → **Create a project**
   - Выберите **Connect to Git**
   - Авторизуйтесь через GitHub и выберите ваш репозиторий
   - Настройки сборки:
     - **Framework preset**: `None` или `Static HTML`
     - **Build command**: (оставьте пустым - проект статический)
     - **Build output directory**: `.` (текущая директория)
     - **Root directory**: (оставьте пустым, если проект в корне репозитория)
       - Если проект в подпапке `vcard-personal-portfolio`, укажите: `vcard-personal-portfolio`
   - Нажмите **Save and Deploy**

3. **Автоматический деплой:**
   - После первого деплоя Cloudflare будет автоматически деплоить при каждом push в выбранную ветку (обычно `main`)
   - Каждый деплой получает уникальный URL для preview
   - Production деплой обновляется автоматически

4. **Настройка домена:**
   - После деплоя перейдите в настройки проекта
   - Добавьте кастомный домен (если нужно)
   - Cloudflare автоматически выдаст URL вида `portfolio-website.pages.dev`

### Способ 2: Прямая загрузка (Upload assets)

1. **Подготовка:**
   - Заархивируйте папку `vcard-personal-portfolio` (все файлы внутри)
   - Убедитесь, что архив содержит все файлы, включая `_headers` и `_redirects`

2. **Деплой:**
   - Зайдите на https://dash.cloudflare.com/
   - Перейдите в **Pages** → **Create a project**
   - Выберите **Upload assets**
   - Загрузите архив через интерфейс
   - Cloudflare автоматически распакует и задеплоит

3. **Настройка домена:**
   - После деплоя перейдите в настройки проекта
   - Добавьте кастомный домен (если нужно)
   - Cloudflare автоматически выдаст URL вида `portfolio-website.pages.dev`

### Способ 3: Через Wrangler CLI

```bash
# Установите зависимости (если используете npm)
npm install

# Установите Wrangler глобально (если еще не установлен)
npm install -g wrangler

# Авторизуйтесь
wrangler login

# Деплой проекта
cd vcard-personal-portfolio
npm run deploy

# Или напрямую через wrangler
wrangler pages deploy . --project-name=portfolio-website
```

## Проверка после деплоя

1. Откройте сайт по выданному URL
2. Проверьте работу навигации
3. Проверьте загрузку всех ресурсов (CSS, JS, изображения)
4. Проверьте работу форм

## Оптимизация

Проект оптимизирован:
- ✅ HTML отформатирован и готов к деплою
- ✅ CSS отформатирован и готов к деплою
- ✅ JS отформатирован и готов к деплою
- ✅ Настроены HTTP заголовки для кеширования (`_headers`)
- ✅ Настроены редиректы для SPA (`_redirects`)
- ✅ `.gitignore` настроен для GitHub

## Дополнительные настройки Cloudflare

В настройках проекта можно включить:
- **Auto Minify**: HTML, CSS, JS (уже минифицировано, но можно включить для дополнительной оптимизации)
- **Brotli**: Сжатие (включено по умолчанию)
- **Rocket Loader**: Ускорение загрузки JS (опционально)

## Troubleshooting

**Проблема:** Сайт не загружается
- Проверьте, что `index.html` в корне проекта
- Проверьте пути к ресурсам (должны быть относительными: `./assets/...`)

**Проблема:** Стили не применяются
- Проверьте файл `_headers` - он должен быть в корне
- Проверьте пути к CSS файлам

**Проблема:** JS не работает
- Проверьте консоль браузера на ошибки
- Убедитесь, что `script.js` загружается

## Структура файлов для деплоя

```
vcard-personal-portfolio/
├── index.html              ✅ Главная страница
├── marketly-gallery.html    ✅ Страница галереи
├── _headers                 ✅ HTTP заголовки (кеширование)
├── _redirects               ✅ Правила редиректов (SPA fallback)
├── wrangler.toml            ✅ Конфигурация Wrangler CLI
├── cloudflare-pages.json    ✅ Дополнительная конфигурация
├── package.json             ✅ Конфигурация npm
├── .gitignore               ✅ Игнорируемые файлы
├── assets/
│   ├── css/
│   │   └── style.css       ✅ Минифицирован
│   ├── js/
│   │   └── script.js       ✅ Минифицирован
│   └── images/             ✅ Все изображения
└── DEPLOY.md                ✅ Инструкция по деплою
```

## Важные файлы конфигурации

- **`_headers`** - Настройки HTTP заголовков для безопасности и кеширования
- **`_redirects`** - Правила редиректов для SPA (все запросы → index.html)
- **`wrangler.toml`** - Конфигурация для Wrangler CLI
- **`cloudflare-pages.json`** - Дополнительные настройки для Cloudflare Pages

## Проверка перед деплоем

- [x] Все файлы отформатированы и читаемы
- [x] Пути к ресурсам относительные (`./assets/...`)
- [x] Файлы `_headers` и `_redirects` в корне проекта
- [x] `index.html` в корне проекта
- [x] Все изображения и ресурсы на месте
- [x] `.gitignore` настроен правильно
- [x] Все изменения закоммичены в Git
- [x] Репозиторий запушен в GitHub

## Важные заметки

⚠️ **Если проект в подпапке репозитория:**
- В настройках Cloudflare Pages укажите **Root directory**: `vcard-personal-portfolio`
- Или переместите все файлы в корень репозитория

✅ **Рекомендуется использовать GitHub + Cloudflare Pages:**
- Автоматический деплой при каждом push
- Preview деплои для pull requests
- История деплоев
- Легкий откат к предыдущим версиям

Все готово к деплою! 🚀

