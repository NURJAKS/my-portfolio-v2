# Быстрый деплой через GitHub → Cloudflare Pages

## Шаг 1: Подготовка Git

```bash
cd vcard-personal-portfolio

# Проверьте статус
git status

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Ready for Cloudflare Pages deployment"
```

## Шаг 2: Создайте репозиторий на GitHub

1. Зайдите на https://github.com/new
2. Создайте новый репозиторий (например: `portfolio-website`)
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

## Шаг 3: Подключите к GitHub

```bash
# Если репозиторий еще не подключен:
git remote add origin https://github.com/YOUR_USERNAME/portfolio-website.git

# Запушьте код
git branch -M main  # Переименуйте ветку в main, если нужно
git push -u origin main
```

## Шаг 4: Деплой на Cloudflare Pages

1. Зайдите на https://dash.cloudflare.com/
2. **Pages** → **Create a project** → **Connect to Git**
3. Авторизуйтесь через GitHub
4. Выберите ваш репозиторий
5. Настройки:
   - **Framework preset**: `None`
   - **Build command**: (пусто)
   - **Build output directory**: `.`
   - **Root directory**: (пусто, если проект в корне) или `vcard-personal-portfolio` (если в подпапке)
6. **Save and Deploy**

## Готово! 🎉

После деплоя ваш сайт будет доступен по адресу:
- `https://YOUR_PROJECT_NAME.pages.dev`
- Или по вашему кастомному домену (если настроен)

## Автоматический деплой

Теперь каждый `git push` будет автоматически деплоить сайт!

```bash
# Внесли изменения
git add .
git commit -m "Update content"
git push

# Cloudflare автоматически задеплоит изменения
```

## Важно

- Убедитесь, что все файлы закоммичены (проверьте `git status`)
- Файлы `_headers` и `_redirects` должны быть в корне проекта
- Все пути к ресурсам должны быть относительными (`./assets/...`)

