# Portfolio Website

Статический сайт-портфолио, готовый для деплоя на Cloudflare Pages.

## 🚀 Быстрый деплой

### Вариант 1: Через Dashboard (Рекомендуется)
1. Зайдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Перейдите в **Pages** → **Create a project**
3. Выберите **Upload assets** или подключите Git репозиторий
4. Если через Git, укажите:
   - **Framework preset**: None
   - **Build command**: (оставьте пустым)
   - **Build output directory**: `.` (текущая директория)
5. Нажмите **Save and Deploy**

### Вариант 2: Через Wrangler CLI
```bash
# Установите зависимости
npm install

# Авторизуйтесь в Cloudflare
wrangler login

# Деплой
npm run deploy
```

Подробная инструкция в файле [DEPLOY.md](./DEPLOY.md)

## Структура проекта

```
vcard-personal-portfolio/
├── index.html              # Главная страница
├── marketly-gallery.html    # Страница галереи
├── _headers                 # HTTP заголовки (кеширование, безопасность)
├── _redirects               # Правила редиректов (SPA fallback)
├── wrangler.toml            # Конфигурация Wrangler CLI
├── cloudflare-pages.json    # Дополнительная конфигурация
├── package.json             # Конфигурация npm
├── .gitignore               # Игнорируемые файлы
├── assets/
│   ├── css/
│   │   └── style.css       # Минифицированный CSS
│   ├── js/
│   │   └── script.js       # Минифицированный JS
│   └── images/              # Изображения
└── DEPLOY.md                # Подробная инструкция по деплою
```

## ✨ Особенности

- ✅ Все файлы минифицированы для оптимизации
- ✅ Настроены HTTP заголовки для кеширования и безопасности
- ✅ SPA роутинг через `_redirects`
- ✅ Конфигурация для Wrangler CLI
- ✅ Готово к продакшену

## 📋 Настройки Cloudflare Pages

Рекомендуемые настройки в Cloudflare Dashboard:
- **Framework preset**: None
- **Build command**: (пусто)
- **Build output directory**: `.` (текущая директория)
- **Root directory**: `vcard-personal-portfolio` (если проект в подпапке репозитория)

## 📝 Конфигурационные файлы

- **`_headers`** - Настройки кеширования и безопасности
- **`_redirects`** - Правила редиректов для SPA
- **`wrangler.toml`** - Конфигурация для Wrangler CLI
- **`cloudflare-pages.json`** - Дополнительные настройки Cloudflare Pages

